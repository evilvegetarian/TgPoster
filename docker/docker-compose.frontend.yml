networks:
  default:
    name: tg

services:
  frontend:
    container_name: frontend
    image: ${TGPOSTER_FRONTEND_IMAGE:-ghcr.io/evilvegetarian/frontend:latest}
    ports:
      - "8077:80"
    environment:
      - API_URL=${API_URL}